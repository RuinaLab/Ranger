
#ifndef _ANKLE_FSM_SENSORS_H

#define _ANKLE_FSM_SENSORS_H

// define the states
enum states {push_off, lifting, up, ready_to_hit, on_ground, ready_to_exit};
// define the inputs
enum sensor {ground, _ground, above_threshold, _above_threshold, hip_threshold, _hip_threshold, stop_command, _stop_command};

void get_ankle_sensor_input(int* sensor_array, int sensor_array_length);

#endif
