
#ifndef _STRUCTURE_H

#define _STRUCTURE_H

// define the states
enum states {push_off, lifting, up, ready_to_hit, on_ground};
// define the inputs
enum sensor {ground, _ground, above_threshold, _above_threshold, hip_threshold, _hip_threshold};

#endif
